import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand Colors
  static const Color primary = Color(0xFF7C3AED);    
  static const Color primaryDark = Color(0xFF6D28D9);  
  static const Color primaryLight = Color(0xFFDDD6FE); 
  static const Color secondary = Color(0xFF1E1E1E);

  // Backgrounds
  static const Color scaffold = Color(0xFFF8F9FA);
  static const Color card = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFF3F0FF);

  // Text Colors
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textWhite = Color(0xFFFFFFFF);

  // Status Colors
  static const Color success = Color(0xFF22C55E);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6);

  // Border / Divider
  static const Color border = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFF3F4F6);

  // Taxi Specific
  static const Color availableDriver = Color(0xFF22C55E);
  static const Color busyDriver = Color(0xFFEF4444);
  static const Color onTrip = Color(0xFF7C3AED);

  // Shadow
  static const Color shadow = Color(0x1A000000);
}